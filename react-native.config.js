//Sau khi config xong mở terminal => gõ "npx react-native-asset" để add file font vào thư mục android
//Vào android => app => src => main => assets => fonts kiểm tra
module.exports = {
    assets : ['./assets/fonts/']
}